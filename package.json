{
  "name": "node-cors-client",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next",
    "start": "next start -p $PORT",
    "build": "next build",
    "export": "npm run build && next export",
    "prettier": "prettier --write {components,pages}/**/*.js next.config.js"
  },
  "engines": {
    "node": "^16.20.0",
    "yarn": "^1.22.4"
  },
  "dependencies": {
    "isomorphic-unfetch": "^3.0.0",
    "next": "^8.1.0",
    "next-env": "^1.1.0",
    "node-cors-client": "file:",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "prettier": "^1.17.1"
  }
}